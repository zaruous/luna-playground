{
  "name": "nyang-token-tracker",
  "private": true,
  "version": "0.4.0",
  "description": "Cat-themed local AI usage tracker with Codex realtime collection",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "vite build",
    "server": "node scripts/server.mjs",
    "start:web": "npm run build && node scripts/server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.4",
    "vite": "^6.4.3"
  },
  "engines": {
    "node": ">=24.17.0"
  }
}
